# Runtime dependencies only (for API deployment)
# Excludes heavy ingest pipeline packages

# API (Phase 9)
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0

# Vector Database (for retrieval)
chromadb>=0.4.0

# LLM (Phase 6)
groq>=0.5.0

# Utilities
python-dotenv>=1.0.0

# Data processing (minimal)
pandas>=2.1.0
